# Copy to .env (same directory as this file's parent, i.e. the repo root) and
# fill in real values:
#   cp deploy/.env.example .env
#
# .env is read by two things:
#  - deploy/ragtrust.service's EnvironmentFile= (systemd), for production.
#  - src/ragtrust/service.py's _LazyASGIApp, which reads these same names
#    straight from os.environ (RAGTRUST_INDEX/RAGTRUST_GENERATOR/RAGTRUST_MODEL)
#    when `uvicorn ragtrust.service:app` is started directly (no CLI flags).
#
# Never commit the real .env -- it holds a live API key. Only this .example
# file (placeholder values only) belongs in git.

# --- LLM backend: OmniRoute, an OpenAI-compatible router running on the VPS
# --- itself (src/ragtrust/generation/openai_compat.py::OpenAICompatGenerator).
RAGTRUST_LLM_BASE_URL=http://127.0.0.1:20128
RAGTRUST_LLM_API_KEY=replace-with-the-real-omniroute-key
# One of the models OmniRoute's GET /v1/models reports, e.g. auto/best-fast,
# auto/best-reasoning, auto/best-coding.
RAGTRUST_LLM_MODEL=auto/best-reasoning

# --- Which generator backend service.py's _make_generator() should build.
# Set to "openai_compat" to use OmniRoute (see above). Other valid values:
# "ollama", "hf_api", "cached" -- see src/ragtrust/service.py::_make_generator.
RAGTRUST_GENERATOR=openai_compat

# --- Path to a saved index directory (built with `ragtrust index <corpus> --out
# --- <dir>`; see deploy/README.md step 3). Absolute path recommended since
# --- systemd's WorkingDirectory makes a relative one resolve from the repo root
# --- anyway, but being explicit here avoids ambiguity.
RAGTRUST_INDEX=/home/admin/projects/rag-trustworthiness-industrial/data/index

# Read timeout in seconds. A router fronting several providers can queue or
# cold-start, so a single completion may exceed a minute; 60s proved too tight
# against a live deployment and surfaced healthy-but-slow as "unreachable".
RAGTRUST_LLM_TIMEOUT=240

# --- Persisted document upload (POST /corpora, src/ragtrust/corpora.py). The
# --- systemd unit grants write access to /var/lib/ragtrust via
# --- StateDirectory=ragtrust + ReadWritePaths=/var/lib/ragtrust (see
# --- deploy/ragtrust.service) -- nowhere else under the hardened sandbox is
# --- writable, so this must point inside it.
RAGTRUST_UPLOAD_DIR=/var/lib/ragtrust/corpora

# Reject an upload above this size before it is fully buffered (service.py
# streams and checks in 1 MiB chunks) or indexed.
RAGTRUST_MAX_UPLOAD_MB=10

# Reject a PDF with more pages than this before extracting any text -- page
# count is checked first because extraction is unbounded work.
RAGTRUST_MAX_PDF_PAGES=100

# How long an uploaded corpus is kept before CorpusStore.sweep() deletes it.
RAGTRUST_CORPUS_TTL_HOURS=24

# Hard cap on the number of uploaded corpora kept on disk at once; sweep()
# deletes the oldest first once this is exceeded, independent of the TTL.
RAGTRUST_MAX_CORPORA=20

# How many uploaded corpora's indexes are kept warm in memory (an LRU) so a
# repeat query against a recently used corpus does not reload it from disk.
RAGTRUST_CORPUS_CACHE=4

# --- Upload abuse protection (src/ragtrust/ratelimit.py), because POST
# --- /corpora is public with no login: it writes to disk and spends CPU
# --- indexing, both of which need a hard ceiling once the service is
# --- reachable from the internet.

# Max uploads a single client may make per RAGTRUST_UPLOAD_RATE_WINDOW.
RAGTRUST_UPLOAD_RATE_LIMIT=5

# Max uploads from ALL clients combined per the same window. Exists because a
# per-client cap alone assumes source addresses are scarce -- under IPv6 one
# host can control a /64 (billions of addresses), so this global cap is what
# actually bounds total disk/CPU spent on uploads.
RAGTRUST_UPLOAD_GLOBAL_LIMIT=30

# Window (seconds) both limits above are measured over.
RAGTRUST_UPLOAD_RATE_WINDOW=3600

# Set to "true" ONLY because Caddy (deploy/Caddyfile.snippet) terminates TLS
# in front of this service and overwrites X-Forwarded-For on every request it
# forwards -- that is what makes the header trustworthy here. Setting this on
# a service reachable directly (no proxy in front of it) would let any client
# put whatever value it wants in that header and forge its own identity,
# making the per-client limit above trivially bypassable. Leave unset/false
# for any deployment where uvicorn is directly reachable.
RAGTRUST_TRUST_PROXY=true

# Optional shared secret. Unset/empty (the default) leaves uploads public --
# required so the demo works for someone who just opens the page with no
# token in hand. Set it to require header `X-Upload-Token: <value>` on every
# POST /corpora; a request with the correct token bypasses both rate limits
# above (it's an authenticated operator, not anonymous traffic).
RAGTRUST_UPLOAD_TOKEN=

# --- /answer abuse protection (src/ragtrust/ratelimit.py), because POST
# --- /answer is also public with no login: it calls a paid, hosted LLM
# --- backend and spends ~14s of CPU on NLI entailment per request, both of
# --- which need a hard ceiling once the service is reachable from the
# --- internet. There is no token bypass for /answer -- every caller is
# --- subject to both limits below.

# Max /answer calls a single client may make per RAGTRUST_ANSWER_RATE_WINDOW.
RAGTRUST_ANSWER_RATE_LIMIT=20

# Max /answer calls from ALL clients combined per the same window. Exists for
# the same reason as RAGTRUST_UPLOAD_GLOBAL_LIMIT above (see its comment): a
# per-client cap alone assumes source addresses are scarce, which IPv6 makes
# untrue, so this is what actually bounds total LLM spend and CPU.
RAGTRUST_ANSWER_GLOBAL_LIMIT=200

# Window (seconds) both limits above are measured over.
RAGTRUST_ANSWER_RATE_WINDOW=3600

# --- FAISS thread cap. torch, sklearn and faiss each vendor their own copy of
# --- libomp, and with more than one in a process FAISS's parallel regions
# --- segfault once an index is big enough for it to spawn threads (reproduced
# --- at 22,878 vectors, absent at 5,183). POST /corpora indexes an arbitrary
# --- uploaded document, so a large enough upload could kill the process.
#
# Caps faiss ALONE. Every index here is an IndexFlat*, whose add and search are
# bound by memory bandwidth rather than thread count, so faiss loses nothing.
#
# Do NOT use OMP_NUM_THREADS for this. It was set here first and that was a
# mistake: OMP_NUM_THREADS caps *every* OpenMP consumer in the process, PyTorch
# included, and PyTorch runs the NLI entailment that dominates this pipeline
# (entail + score were 79s of a 99s answer in production). On the deployed host
# -- which has 8 cores, not the 2 an earlier comment here claimed -- that
# throttled the whole service to one core to protect an index operation that
# was never the bottleneck. Startup alone went 85s -> 40s on removing it.
#
# Why this is a variable rather than an unconditional call in code:
# faiss.omp_set_num_threads() is what forces faiss's libomp to initialise, and
# on macOS that aborts the process with `OMP: Error #15`. Linux does not trip
# that check, so setting it here is safe for this deployment and stays opt-in
# everywhere else. See src/ragtrust/retrieval/index.py::import_faiss.
RAGTRUST_FAISS_THREADS=1
